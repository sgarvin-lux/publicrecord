name: Ingest HCRIS Cost Reports

on:
  schedule:
    # Run quarterly on the 1st of Jan, Apr, Jul, Oct at 3am UTC
    - cron: "0 3 1 1,4,7,10 *"
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: pip install pandas requests

      - name: Parse HCRIS data
        run: echo "TODO - implement HCRIS cost report parsing"
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
